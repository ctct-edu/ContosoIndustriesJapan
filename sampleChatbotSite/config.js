/* ============================================================
   接続設定ファイル
   Lab04 で取得した Azure AI Foundry の情報をここに書き込みます。
   << >> で囲まれた部分を、自分の環境の値に置き換えてください。
   ============================================================ */

const CONFIG = {
  // --- ここは必ず書き換える -----------------------------------

  // エンドポイント（末尾の / は付けない）
  // 例: https://aif-intern-260401-01.openai.azure.com
  ENDPOINT: "<<エンドポイントを貼り付け>>",

  // モデルのデプロイ名
  // 本研修では全員 "gpt-5.4-nano" で統一するため、書き換え不要です。
  DEPLOYMENT: "gpt-5.4-nano",

  // API キー
  API_KEY: "<<APIキーを貼り付け>>",

  // --- ここから下は必要に応じて変更 ---------------------------

  // API のバージョン（通常は変更不要）
  API_VERSION: "2024-10-21",

  // AI の役割・話し方を決める指示
  SYSTEM_PROMPT:
    "あなたは工場の IT 担当者を手助けするアシスタントです。専門用語はできるだけかみ砕き、日本語で簡潔に答えてください。",

  // 画面を開いたときに最初に表示するメッセージ
  GREETING: "こんにちは。聞きたいことを入力してください。",

  // 画面上部の見出し
  TITLE: "AI チャット",
  SUBTITLE: "Azure AI Foundry に接続しています",

  // 1 回の応答の最大長さ
  MAX_TOKENS: 800,

  // 応答のばらつき（0 に近いほど毎回同じような答えになる）
  TEMPERATURE: 0.7,

  // AI に渡す過去のやり取りの数（多いほど文脈を覚えるが料金も増える）
  HISTORY_LIMIT: 20,

  // gpt-5 系のモデルを使う場合は true にする
  // （パラメーター名が異なり、TEMPERATURE も指定できないため）
  // 本研修では gpt-5.4-nano で統一するため、書き換え不要です。
  IS_GPT5: true,
};
